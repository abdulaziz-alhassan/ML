For this Lab i learned how to make and train machine learning project using KNN model which classify artificial data point into two target classes and performed eda to asess the data value and check forpatterns and evaluation using Classification Reportand confusion matrix and used Standardscaler because KNN is a distance-based algorithm, scaling is mandatory to prevent features and the eldow method which iterated through K-values from 1 to 40 to find the optimal "elbow" which in this case was K=30.